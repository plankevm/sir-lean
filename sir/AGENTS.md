# Agent Rules

- Keep agent rules minimal: use simple bullet points and do not over-explain.
- Never add redundant comments; code documents itself through names, type signatures, structure names, and theorem names.
- Keep `README.md` maintained as a high-level overview only; exclude implementation details.
