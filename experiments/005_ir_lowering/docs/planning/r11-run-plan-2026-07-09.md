# R11 next-run plan — 2026-07-09 evening

## Overnight ledger — 2026-07-10 night (session-limit pause; resumes ~11:30 São Paulo)

Debt at pause: **7 sorry-carrying declarations** (Producer 5: simStmt_coupled_call,
simStmts_coupled_block, driveLogStep_of_block, runFrom_of_driveCorrLog_rec,
runFrom_of_driveCorrLog; RealisabilitySpec 2: stmtTies'_of_runWithLog, lower_conforms_exact).
Machinery is sorry-FREE; **callRealises_of_recorded is CLOSED and axiom-clean** (R3 done
end-to-end). Also closed today: R12a witness (kernel-certified via checked-twin evaluator,
#guard_msgs axiom guard), realisedGas_nil_of_noGasReads (chunk 8 companion),
exact-consumption terminal bricks (chunk 7 kernel), chunk-4 gas/sload arms, scoped-Corr
carrier + ScopedUses statics, R6 geometry, statics extension (call/create charge arms +
result slotAddr, Eduardo-approved, with named derivation lemmas in BudgetDerivations).

Pending at resume (in order):
1. DONE (2026-07-10): re-review (statement honesty) of the limit-interrupted merges 63a3079b
   (call bricks) and 3535ef4c (call_head_realises_coupled salvage). Verdict PASS-WITH-NOTES —
   both statement-honest (no weakened statements, no free-∀ tie, no vacuity), and all four call
   theorems independently confirmed axiom-clean ([propext, Classical.choice, Quot.sound]). NB
   no build gate enforces this (Machinery only builds under WIP), so it was checked directly.
   Only finding: 7 stale "[named WIP]" labels on now-closed theorems (understatement, not
   overclaim) — swept in Machinery.lean.
2. Resume/redispatch coupled-arms: simStmt_coupled_call (partial uncommitted work preserved
   in .claude/worktrees/wf_b849c4f8-8a8-2 — Producer.lean modifications on top of the
   salvaged Machinery commit), CREATE finish half, simStmt_coupled_create.
3. Terminal wave: stmtTies'_of_runWithLog, block-walk trio, runFrom_of_driveCorrLog,
   exact flagship; plus the threaded-hypothesis → derivation-lemma swap (statics landed)
   and the stale Machinery docstrings (~2905/3251) cleanup.
4. SLOAD-warmth-tracking REMOVAL (new, 2026-07-10; sequence AFTER the terminal wave — it
   reshapes the core driveLog tuple the open proofs are currently written against, so doing it
   mid-wave would churn signatures). Evidence (Opus investigation, 2026-07-10): the recorded
   `log.sloads` stream is VESTIGIAL. Storage is not an oracle — the SLOAD value is reflexive
   (Semantics.lean:39, `.sload k => st.world key`) and the warmth charge is already subsumed
   into the reflexive `realisedGas` stream (a later GAS read records `exec.gasAvailable`, which
   already reflects every warmth charge the bytecode paid). Smoking guns: `realisedSload` has
   ZERO call sites; `EvalStmt`/`RunFrom` thread no sload stream; `sloadChg : Tmp → ℕ` is a free
   ∀-parameter never pinned to recorded warmth (only its `chargeCache … .length ≤ 1024` bound
   is load-bearing, and that length is sloadChg-independent); neither flagship mentions
   `realisedSload`. It survives by inertia from the retired `SloadRealises` value-universal
   (introduced 1dfd45fa 2026-06-27; its siblings GasRealises/SstoreRealises were already swept).
   Refactor = mechanical 5-tuple→4-tuple shrink across `driveLog` and every lemma matching its
   tuple (RecorderCoupled/DriveCorrLog/restart, CheckedStep/SegmentedEval/WitnessChecks
   destructurings) + genuine delete of the SloadRealises/SloadLogAligned/
   sloadRealises_charge_of_witness cluster (SelfPresent.lean / MaterialiseRuns.lean). Also drop
   sloadWarmthOf / isSloadOp / realisedSload / driveLog's sloadAcc + its
   `isSloadOp && stack.isEmpty` splice branch (Recorder.lean:80-82). Removing `log.sloads` does
   NOT touch the SelfPresent account-presence residue.

### Fan-out progress — 2026-07-10 (parallel tracks A/B/C off main 6d219f75)

- **A `simStmt_coupled_call` — CLOSED, axiom-clean** ([propext, Classical.choice, Quot.sound]),
  statement unchanged (only unused-hyp rename hties→_hties). Assembles via the closed
  `call_head_realises_coupled` + a here-constructed Route-B tail. Staged for integration
  (Producer 5→4 sorries). Salvaged sim_call_stmt'/call_post_wellScoped' from the old worktree.
- **B `stmtTies'_of_runWithLog` — 4/6 arms CLOSED, 2 BLOCKED** (partial kept on its branch;
  NOT merged, so main's stmtTies' stays a single sorry until fully closed). Closed: arm 1
  (plain assign), arm 3 (gas, via gas_suffix_head_realised), arm 4 (sstore), arm 5 (call, via
  the closed callRealises_of_recorded). Correction: on main `StmtTies'` is the 6-arm,
  create-aware bundle. The two blockers are the remaining stmtTies' work:
  - **Arm 2 (sload activeWords-flatness):** needs a new `activeWords`-eq field on `MatRunsC`
    (MatFoldChannel.lean ~802) — it records `memActive ≤` not `activeWords =`. Default-cone
    edit, reportedly rfl-preserving. ← dispatched 2026-07-10.
  - **Arm 6 (`CreateRealisesS`):** needs `createRealises_of_recorded` + CREATE Piece B (mirror
    of the CALL bricks; partly default-target resumeAfterCreate pins). SAME machinery
    `simStmt_coupled_create` needs — this is the chunk-3 CREATE workstream, now confirmed on
    the flagship critical path.
- **C block-walk trio (simStmts_coupled_block/driveLogStep_of_block/runFrom_of_driveCorrLog_rec)**
  — running (Producer 1025+ region, fenced from A).

Net after A integrates: 7→6. Flagship critical path now clearly routes through the CREATE
run producer (blocks both stmtTies' arm 6 and simStmt_coupled_create).

### Round 2 + integration — 2026-07-10

Branches (local, not pushed): `exp005-r11-fanout` (14d4fbfd, A+C), `exp005-r11-stmtties-partial`
(1d9cfe35, B 4/6), `exp005-r11-stmtties` (6c624ba7, +arm2+MatRunsC = 5/6), `exp005-r11-create`
(63b30907, CREATE foundation), **`exp005-r11-integrated`** = create ⊕ stmtties (clean merge, ort;
build verifying) — the single integration line for round 3.

Closed this session (all axiom-clean where a headline): simStmt_coupled_call (#1),
runFrom_of_driveCorrLog_rec (#4), stmtTies' arm 2 (+ MatRunsC.activeWordsEq field, default cone
still sorry-free), CREATE bricks create_args_run_of_coupled + create_tail_of_cleanHalt.

Open decls on integrated (9): Producer 4 (simStmts_coupled_block #2, driveLogStep_of_block #3,
runFrom_of_driveCorrLog #5, simStmt_coupled_create-stub), Machinery 3 (create_dispatch_of_coupled,
createRealises_of_recorded, create_head_realises_coupled — all stubs), RealisabilitySpec 2
(stmtTies' create-arm only, lower_conforms_exact flagship). Count rose 7→9 because the CREATE
skeleton is now explicit (was latent); 2 of its bricks already closed.

CREATE debt boundary (build these first, next-agent order): (1) `stepFrame_create2` (forward
CREATE2 dispatch producer, twin of stepFrame_call) + `create_extraCost_le_of_cleanHalt` (CREATE
clean-halt gas brick); (2) `driveLog_creates_const_of_depth` + close create_dispatch_of_coupled;
(3) the 3 resumeAfterCreate stack/mem/activeWords pins; (4) close createRealises_of_recorded +
create_head_realises_coupled (assembly); (5) sim_create_stmt' + transcribe simStmt_coupled_create.

Round 3 (off exp005-r11-integrated): **X** = CREATE continuation (the debt-boundary order above);
**Y** = block-walk reshape #2/#3/#5 (thread codeFits + CallsCode seam from boundaryWalk_of_wl,
re-prove the closed #4 with the added hyps, close #2 consuming the wired simStmt_coupled_create,
#3 via new coupled-terminator bricks, #5). X unblocks stmtTies' arm 6 + simStmt_coupled_create.

## Wave checkpoint — 2026-07-09 late evening (WAVE CLOSED)

All three tracks merged and locally verified (both builds green on the final tree);
worktrees removed. WIP debt = 12 proof-position sorries (Machinery 1, Producer 7,
RealisabilitySpec 4). Final history: `d95d178` (box integration) → `7395080` (R6 closed) →
`9131a0e` (create-suffix reshape) → `ccbbb3f` (R3 finish half) → signature repair
(`callRealises_of_recorded_finish` re-threaded for the enlarged `RecorderCoupled` — the CALL
worker proved against the pre-createSuffix coupling; the full-tree gate caught it).

Next dispatch queue:
1. **Shared argument-push producer**: CALL Piece B proper (the cursor bundle consumed by the
   closed `callRealises_of_recorded_finish`) and the create realisation bridge +
   `simStmt_coupled_create` are the same pattern (materialise-args run → decode the
   call/create frame → rule out the degenerate branch → hand to finish half). One run should
   own the shared machinery. Note from the CALL run: the theorem site needs the flagship's
   `codeFits` scalar for CALL-site decode bounds.
2. **Chunk 4 coupled gas/sload arms**: GATED on Eduardo signing off the scoped-`Corr` (R0b)
   decision in `gas-sload-invariants-design-2026-07-09.md`.
3. Then chunks 5–8 (block walk, producer, flagship shells) per the dispatch order below.

- Phase 0 done: box integration merged (`d95d178`), tree verified identical to box tip.
- R6 (chunk 1) CLOSED: strict-bound + whole-walk sorries gone (`7395080`,
  new `Decode/BoundaryCursor.lean` brick, Machinery net shrink).
- Create channel (chunk 3) PART 1 landed (`9131a0e`): `RecorderCoupled`/`DriveCorrLog`/
  `StreamsAligned` thread a real `createSuffix` (whole-`log.creates` pinning eliminated),
  transported through all existing coupled arms, no new debt. Remaining chunk-3 blocker:
  the create realisation bridge (recorded head → `CreateReturns` resume) +
  `simStmt_coupled_create` — dispatch as a CALL-Route-B-pattern continuation once chunk 2 lands.
- CALL Piece B (chunk 2): run in flight (worktree `evm-wt-call`).
- GAS/SLOAD (chunk 4 prereq): design note delivered —
  [`gas-sload-invariants-design-2026-07-09.md`](gas-sload-invariants-design-2026-07-09.md).
  KEY FINDING: `simStmt_coupled_gas`/`_sload` are UNPROVABLE AS STATED (strong `Corr.defsSound`
  machine-checked false mid-block after a rebind); fix = the R0b scoped-`Corr` reshape
  (`DefsSoundS` indexed by the `invalStep` fold). CALL/CREATE result temps flagged as the same
  species. Chunk-4 dispatch GATED on Eduardo's sign-off of the note's decision section.

Companion to [`r11-plan-2026-07-08.md`](r11-plan-2026-07-08.md) (chunk definitions, gates,
no-gos all still apply). This document consolidates the terminal outcomes of the four box
tracks, the verified local git truth, and the dispatch order for the next Smithers passes.

## Verified ground truth (local, 2026-07-09)

Checked directly against the local object store; supersedes the run reports' "not yet imported"
phrasing and the 07-08 checkpoint's "commits do not exist locally" finding.

- The box commits ARE fetched locally under `refs/remotes/box/`:
  `box/r11-create2-only` = `239f873`, `box/r11-call-piece-b` = `e90b3c9`. Nothing is merged.
- **The box line diverged from local main.** Both box branches sit on a 17-commit stack whose
  first five commits are rebased twins (same subjects, different hashes) of the five boundary
  commits on local main (`e210b3a..c760145`). Above the twins, the box line carries the R6
  pc-classifier work — `0571ec44..ff825e3b` (engine/JUMPDEST/arith/push/CALL/create pc facts,
  lowered step pc classifier) and `9d45927` (call boundary in-range) — ~1,945 insertions,
  dominated by `Engine/StepWalk.lean` (+887) and `Machinery.lean` (+102).
- Track tips have **zero file overlap** beyond the shared base:
  - CREATE2 (`4a0d57e8` + `239f873`): `Spec/{IR,Lowering,Semantics,WellFormed}.lean`,
    `Assembly/LowerConforms.lean`, `Decode/SegAligned.lean`, `Engine/Modellable.lean`,
    `Materialise/DefsSound.lean`, `V2/{Drive/DriveSim,Law,Realisability/Witness}.lean` + docs.
  - CALL (`e90b3c9`): `V2/Realisability/Machinery.lean` only (+45, three helper lemmas;
    headline `callRealises_of_recorded` still open).
- Terminal track outcomes: CREATE2-only strongest (default build reported green box-side);
  CALL partial (helpers only, not independently build-verified); R6 latest pass blocked on a
  cursor-preserving strict-bound theorem (no new commit); GAS/SLOAD blocked on missing
  state-indexed producer invariants (no code changes).
- An integration run (`run-1783631244267`, gpt-5.6-sol, box `bx_jpavy439`, branch
  `codex/r11-box-integration`) is active. It is box-side only: the run does not appear in the
  local Smithers DB and `codex/r11-box-integration` is not fetched locally.

## Risks specific to this cycle

1. **Archive race.** `bx_jpavy439` archives ~22:21–22:27 UTC today while hosting the live
   integration run; the integration branch exists nowhere else. Snapshots are complete, so
   work is recoverable, but the run can be interrupted. Mitigation: fetch
   `codex/r11-box-integration` to the local `box` remote as soon as the run pushes anything,
   and again immediately when it finishes; do not let the only copy sit on an archiving box.
2. **Duplicate-twin merge trap.** A naive merge of the box stack onto main will conflict on or
   duplicate the five twin commits. Integration must rebase only `main..9d45927`'s
   *non-twin* range (or cherry-pick `87394465..9d45927` + track tips) onto local main, then
   verify the twins were content-identical (diff the patch-ids; investigate any drift).
3. **CREATE2 rewrites the Spec surface** (`IR.lean`, `Lowering.lean`, `Semantics.lean`,
   `WellFormed.lean`). Per the 07-08 plan this is the sanctioned CREATE2-only correction
   (mandatory salt, no `Byte.create` branch), but it invalidates line anchors in the 07-09
   review tour and may ripple into WIP statements; the sorry recount after import is mandatory.

## Dispatch order

**Phase 0 — integration lands (in flight).** Exit gates unchanged: deliberate merge against
base `9d45927`, both builds green, sorry recount, no new public premises. On completion, fetch
the integration branch locally, verify locally (`lake build` + `lake build WIP` run *locally*,
not trusted from the box), then fast-forward or merge to main. If the run dies to the archive,
restore from snapshot and rerun with the same brief; do not hand-merge under time pressure.

**Phase 1 — CREATE2 repair (chunk 3 opener).** One pass: fix the remaining `Machinery.lean`
CREATE2 salt case on top of the integrated tree. Then the create coupled channel per chunk 3
(create suffix in `RecorderCoupled`/`StreamsAligned`, `StmtTies'` create arm,
`simStmt_coupled_create`). This is the critical path: it changes coupling signatures that
chunks 4–6 consume, and it is the latent obligation with no sorry placeholder.

**Phase 2 — CALL Piece B continuation (chunk 2).** Independently build-verify `e90b3c9`
(never was), then a fresh pass on `callRealises_of_recorded` consuming the three helpers.
No-gos unchanged: no single-call restriction, no supplied per-call tie.

**Phase 3 — design-first passes for the two blocked tracks.** Both stalled on missing
*statements*, not hard proofs; do not redispatch blind grinding:
- **R6**: a short pass whose first deliverable is the cursor-preserving strict-bound theorem
  *statement* (reviewed before proving). New Decode/Engine bricks stay narrow and proof-facing.
- **GAS/SLOAD**: a design note on the state-indexed producer invariants (what state indexes
  them, how they thread through `DriveCorrLog`) gated on human review, then the proof pass.
  Two consecutive blocked passes mean the invariant shape is the bottleneck, not effort.

**Phase 4 — bookkeeping gate (blocking, before any chunk-4 dispatch).**
- Update the 07-08 plan checkpoint: replace the "four runs own disjoint work" section with
  terminal outcomes and this document's ground truth (the old checkpoint cites box hashes
  without noting they are box-remote-only; that ambiguity cost a review cycle).
- Recount WIP debt (was 16 sorries / 15 decls + latent create channel; CREATE2 import moves
  both numbers).
- Spec/comment cleanup pass per the standing cleanup gate (consume-or-delete new helpers,
  e.g. the ~500 lines of staged boundary support only partially consumed, and the twin-commit
  provenance notes).

**Parallelism:** Phases 1 and 2 may run in separate worktrees (zero file overlap) once Phase 0
lands. Phase 3's two design passes are independent of each other and of 1/2, but their *proof*
passes must rebase onto whatever chunk-3 signature changes land first. Phase 4 is serial.

## Standing constraints (unchanged, restated for dispatch briefs)

Green gates from the 07-08 plan apply to every pass. No public premise may mention internal
adapters, supplied ties, call-count restrictions, or new size premises. Support lemmas count as
progress only when a named WIP sorry consumes them. The post-R11 design queue (annotated
emission, checked lowering wrapper, SSA block parameters) stays frozen until the producer
closes.
