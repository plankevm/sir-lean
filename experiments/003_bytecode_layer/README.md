# Experiment 003 — folded into EVM

The maintained bytecode reasoning layer now lives in
[`EVM/BytecodeLayer/`](../../EVM/BytecodeLayer/). This directory retains the
experiment's historical plans and reports.
