# Roadmap / thread tracker — 2026-07-15

Live tracker of parallel work threads so parked items aren't forgotten. Each has a **status** and, if
parked, a concrete **reactivation trigger** — the observable condition that means "pick this up now."

Context: the bytecode-layer consolidation is done and on `main` (exp003 + exp005 engine folded into
top-level `EVM/BytecodeLayer`, IR-agnostic; the three `Lir.lower_conforms{,_exact,_gasfree}` flagships
closed + axiom-clean). philogy is building the canonical `sir/` compiler on `main` (PR #7 block-IO
merged). Division of labor: **philogy owns the IR stack + scheduling (his pipeline steps 1–4); we own
assembly + bytecode + conformance (steps 5–6)**; the join is the 4→5 seam.

## Track P — philogy's critical path (his; we support, don't touch)
His near-term: internal functions in the IR def/semantics; text-format parser + CLI; memory semantics.
Then: lowering + next IR in the stack, well-formedness, basic theorems. **Our role:** stay current,
unblock, answer bytecode-layer questions, be ready to own the lowering. Eduardo also contributes here
directly. The autonomous box work (Track E) exists precisely to keep our-lane grind off Eduardo's plate
so he has bandwidth for this.

## Track E — our concrete lane (non-speculative, IR-independent, runs in parallel)
| Item | Status | Notes |
|---|---|---|
| Merge overnight stack (`asm-sole-spine` + `overnight-polish`) → main | in progress | rebased onto main; ff after green verify |
| Instruction reuse: `Asm.Op` → `Evm.Operation`/`Instr.lean` | **queued (discuss first)** | make the assembler's ENCODE side speak the canonical EVM instruction type (DECODE already does). IR-independent; needed the moment any IR emits memory ops / more opcodes. The concrete kernel of philogy's "reuse Instr.lean" — NOT the vetoed typeclass. Good green-gated box workflow. |
| SIR retarget deep-prep | ongoing (non-committal) | keep reading SIR's evolving semantics; refine conformance sketch (see [sir-smallstep-bridge assessment](./sir-smallstep-bridge-assessment-2026-07-15.md)) so we move instantly when his IR settles. |

## Track B — parked backlog (documented, with triggers)
| Item | Status | Reactivation trigger |
|---|---|---|
| **SIR lowering + conformance retarget** (our big contribution; absorbs the value-channel generalization `GenStorageAgree`/`GenMemRealises`) | parked | philogy's IR reaches lowering-readiness (after internal functions + memory semantics land and stabilize) |
| **Call/halt/context typeclass abstraction** | parked | philogy has 2–3 high-level IRs and finds the commonality empirically (his own criterion). Do NOT build speculatively. |
| **Static/delegate call-context semantics + `static ⇒ write ⇒ halt` invariants** | parked | philogy's lowering actually emits static/delegate calls. (Vendored EVM has the mechanism `canModifyState`; we've never exercised or surfaced it.) |
| **A2 StackUnderflow tag rename** | parked (supervised) | opportunistic; touches vendored `ExecutionException` + SharedObservable — needs eyes |
| **Worktree/branch cruft sweep** (~20 `worktree-*` + stale branches) | parked (supervised) | opportunistic; risk of unpushed work, do carefully |

## Coordination note
The one thing worth early alignment with philogy (not abstracting — just agreeing on shape): the
**handoff interface at the 4→5 seam** — the `AsmProgram` representation and the high-level EVM-machine
surface (extcall / halt / call-context). Everything above/below can evolve independently; that contract
is shared by every IR he lowers. The instruction-reuse item is the first concrete step toward making
that seam clean.
